<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.jonassjoberg.phonesagainsthumanity.MainActivity" >

    <Button
        android:id="@+id/buttonJoin"
        style="@style/ButtonText"
        android:layout_width="match_parent"
        android:layout_above="@+id/buttonHost"
        android:background="@drawable/buttons"
        android:onClick="joinActivity"
        android:text="Join" />

    <Button
        android:id="@+id/buttonHost"
        style="@style/ButtonText"
        android:layout_width="match_parent"
        android:background="@drawable/buttons"
        android:layout_above="@+id/buttonDownload"
        android:onClick="hostActivity"
        android:text="Host" />
    
    <Button
        android:id="@+id/buttonDownload"
        style="@style/ButtonText"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:background="@drawable/buttons"
        android:onClick="startDownload"
        android:text="Download standard decks" />

</RelativeLayout>