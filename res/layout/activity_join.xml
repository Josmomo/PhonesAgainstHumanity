<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" 
    android:background="@drawable/background">

    <!-- 
    <Button
        android:id="@+id/test"
        style="@style/ButtonText"
        android:layout_width="match_parent"
        android:layout_above="@+id/buttonBluetoothSearch"
        android:background="@drawable/buttons"
        android:onClick="testThis"
        android:text="Test" />
    
    --> 
    <Button
        android:id="@+id/buttonBluetoothSearch"
        style="@style/ButtonText"
        android:layout_width="match_parent"
        android:layout_centerInParent="true"
        android:background="@drawable/buttons"
        android:text="Search for devices" />

    <ListView
        android:id="@+id/listViewBluetoothSearchResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonBluetoothSearch"
        android:stackFromBottom="false"
        android:transcriptMode="alwaysScroll" >
    </ListView>

</RelativeLayout>